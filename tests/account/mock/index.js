import {
  MOCK_USER,
  MOCK_IMAGE_ENCODED,
  UPDATE_MOCK_USER,
} from '../../user/mock';

const NEW_PASSWORD = 'new password';

module.exports = {
  MOCK_USER,
  MOCK_IMAGE_ENCODED,
  NEW_PASSWORD,
  UPDATE_MOCK_USER,
};
